# JOY N COLLECTIVE

Drag this project into GitHub as a repository, then import the repository into Vercel.

Run locally: `npm install` then `npm run dev`.

This first package is a branded functional storefront starter with temporary images and a browser-persisted cart. The next phase can add Supabase, payments, checkout, authentication, and admin inventory.
