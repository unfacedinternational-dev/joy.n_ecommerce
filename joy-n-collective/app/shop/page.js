"use client";
import {useState} from "react"; import Header from "../../components/Header"; import Footer from "../../components/Footer"; import ProductCard from "../../components/ProductCard"; import {products} from "../../data/products";
export default function Shop(){const [filter,setFilter]=useState("All"),[q,setQ]=useState("");const cats=["All",...new Set(products.map(p=>p.category))];const shown=products.filter(p=>(filter==="All"||p.category===filter)&&p.name.toLowerCase().includes(q.toLowerCase()));return <><Header/><main className="shop page"><div className="pageTitle"><p className="eyebrow">JOY N COLLECTIVE</p><h1>Shop All Finds</h1><input placeholder="Search products..." value={q} onChange={e=>setQ(e.target.value)}/></div><div className="filters">{cats.map(c=><button className={filter===c?"active":""} onClick={()=>setFilter(c)} key={c}>{c}</button>)}</div><div className="grid">{shown.map(p=><ProductCard p={p} key={p.id}/>)}</div></main><Footer/></>}
