"use client";
import {useState} from "react"; import Header from "../../components/Header"; import Footer from "../../components/Footer"; import {useStore} from "../../components/Store"; import {peso} from "../../data/products";
export default function Checkout(){const {cart,setCart}=useStore();const [done,setDone]=useState(false);const total=cart.reduce((s,i)=>s+i.price*i.qty,0);function submit(e){e.preventDefault();const f=new FormData(e.currentTarget);const order={id:"JNC-"+Date.now(),date:new Date().toLocaleString(),name:f.get("name"),email:f.get("email"),address:f.get("address"),payment:f.get("payment"),items:cart,total};const orders=JSON.parse(localStorage.getItem("jnc_orders")||"[]");localStorage.setItem("jnc_orders",JSON.stringify([order,...orders]));setCart([]);setDone(true)}if(done)return <><Header/><main className="success"><h1>Thank you for your order!</h1><p>Your order has been recorded. We will contact you regarding fulfillment.</p><p>For this starter version, payment methods are operational as order selections; connect your payment gateway before accepting live online card payments.</p></main><Footer/></>;return <><Header/><main className="checkout"><form onSubmit={submit}><h1>Checkout</h1><label>Full Name<input name="name" required/></label><label>Email<input name="email" type="email" required/></label><label>Phone<input name="phone" required/></label><label>Delivery Address<textarea name="address" required/></label><label>Payment Method<select name="payment"><option>Cash on Delivery</option><option>GCash / Manual Payment</option><option>Bank Transfer</option></select></label><button className="primary dark" disabled={!cart.length}>PLACE ORDER</button></form><aside className="summary"><h2>Your Order</h2>{cart.map(i=><p key={i.id}>{i.name} × {i.qty} <b>{peso(i.price*i.qty)}</b></p>)}<hr/><h3>Total: {peso(total)}</h3></aside></main><Footer/></>}
